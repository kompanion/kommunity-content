{
  "name": "@kompanion/kommunity-content",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "format": "prettier --write {content,paths}/**/*.json"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^1.3.1",
    "prettier": "^1.16.4",
    "pretty-quick": "^1.10.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
